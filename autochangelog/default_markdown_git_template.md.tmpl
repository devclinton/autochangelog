Changelog
=========
{% for version, entries in items.items() %}
* [{{ version }}]{% endfor %}

{% for version, entries in items.items() %}
== {{ version }}
{% for entry in entries %}
* [{{entry.short_id}}]({{ {{entry.id}}) - {{ entry.message|replace("\n", "")|replace("\r", "")|trim }}{% endfor %}

{% endfor %}